# アプリ
PORT=8080
ENV=development

# app サービス用 DB 接続設定（docker-compose.yml の environment と同じキー）
DB_USER=user
DB_PASS=password
DB_NAME=mydb
DB_HOST=db

# mysql サービス用設定（docker-compose.yml の environment と同じキー）
MYSQL_ROOT_PASSWORD=root_password
MYSQL_DATABASE=mydb
MYSQL_USER=user
MYSQL_PASSWORD=password